BackEnd & FrontEnd
   Step One: Build a Dockerfile that has all the configurations and dependecies to run our spring boot application
        - Dockerfile is used to create an image inside of a container

Database Container
    Step One: Build a yaml file

From your project directory, start up your application by running *docker compose up*